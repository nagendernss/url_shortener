services:
  - type: web
    name: url-shortener-java
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: MONGODB_URI
        sync: false      # set in the Render dashboard (Atlas connection string)
